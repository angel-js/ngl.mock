{
  "name": "ngl.mock",
  "version": "0.6.3",
  "devDependencies": {
    "del": "^2.2.0",
    "expect.js": "^0.3.1",
    "gulp": "^3.9.1",
    "gulp-eslint": "^2.0.0",
    "gulp-mocha": "^2.2.0"
  },
  "scripts": {
    "setup": "npm install",
    "upgrade": "npm update",
    "lint": "gulp lint",
    "test": "gulp test",
    "build": "gulp build"
  }
}
